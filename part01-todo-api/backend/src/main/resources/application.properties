# --- Common Settings ---
# This setting applies to all environments.
spring.application.name = todo-api

# --- Profile Activation ---
# This is the most important line.
# It tells Spring Boot to activate the 'dev' profile and load application-dev.properties.
spring.profiles.active = dev
